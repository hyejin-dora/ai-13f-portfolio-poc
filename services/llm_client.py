"""Gemini API 연동 모듈.

역할:
    portfolio_analysis가 만들어 낸 분석 결과(숫자와 표)를 Gemini API에 전달해
    비전문가도 이해할 수 있는 자연어 설명으로 바꾸는 책임을 맡습니다.

포함 예정 기능:
    - Gemini 클라이언트 생성 및 모델 설정
    - 분석 결과를 프롬프트 문장으로 변환
    - 생성된 설명 텍스트 반환 및 오류 처리

주의:
    - API 키는 코드에 직접 쓰지 않고 st.secrets 등 외부 설정에서 읽어옵니다.
    - LLM이 만든 설명에는 사실과 다른 내용이 섞일 수 있으므로 화면에 주의 문구를 함께 표시합니다.
    - 이번 단계에서는 실제 API 호출을 구현하지 않습니다.
"""
